{
  "name": "doc2mcp-server",
  "version": "0.1.0",
  "description": "Stdio MCP proxy for doc2mcp hosted endpoints — run with npx in Cursor or Claude Desktop.",
  "type": "module",
  "bin": {
    "doc2mcp-server": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "pnpm build"
  },
  "keywords": [
    "mcp",
    "doc2mcp",
    "documentation",
    "stdio"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
